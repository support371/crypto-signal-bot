fastapi
uvicorn[standard]
pydantic>=2.0
pydantic-settings>=2.0
pyyaml
python-dotenv
requests
websockets
prometheus_client
httpx
sqlalchemy>=2.0

# Testing (also needed in CI)
pytest
pytest-asyncio

# Optional: required only when TRADING_MODE=live
# Install with: pip install ccxt
# ccxt
