fastapi
uvicorn[standard]
pydantic>=2.0
pydantic-settings>=2.0
pyyaml
python-dotenv
requests
websockets
prometheus_client
httpx
sqlalchemy>=2.0
aiosqlite
asyncpg

# Production server
gunicorn

# Redis (rate limiting, kill-switch, pub/sub)
aioredis>=2.0

# DB migrations
alembic

# Testing & linting (also needed in CI)
pytest
pytest-asyncio
ruff>=0.4

# Live trading execution via ccxt
ccxt>=4.3.0
