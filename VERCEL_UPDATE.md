# Vercel update

Set this environment variable for Production, Preview, and Development unless a different preview backend is intentionally used:

```text
VITE_BACKEND_URL=https://crypto-signal-bot-api.workers.dev
```

After saving the variable, trigger a Vercel production redeploy for the `crypto-signal-bot` project.
