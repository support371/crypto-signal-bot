version: 2.1

jobs:
  runtime-certification:
    docker:
      - image: cimg/node:22.12.0
    steps:
      - checkout
      - run:
          name: Verify deployed paper certification release
          command: node scripts/verify-deployed-paper-release.mjs

workflows:
  runtime-certification:
    jobs:
      - runtime-certification
