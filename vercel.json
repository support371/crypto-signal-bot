{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": null,
  "installCommand": "npm install --no-audit --no-fund",
  "buildCommand": "mkdir -p lock-artifact && cp package-lock.json lock-artifact/package-lock.json && printf '<!doctype html><title>frontend lock artifact</title><p>package-lock generated</p>' > lock-artifact/index.html",
  "outputDirectory": "lock-artifact"
}
